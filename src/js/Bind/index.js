import React from 'react';
import PropTypes from 'prop-types';

const Bind = props => {
    return (
        <div>
            Bind Page
        </div>
    );
};

Bind.propTypes = {
};

export default Bind;